# Expo 资产来源导入入口

接线与 model／Panel 已合入固定候选 `c1486996`，全量类型检查与 R1 浏览器 12 项功能检查通过，尚未发布。财务页的「更新资产来源」接入 `FinanceSourceImportPanel`，传入 `onBack` 和 `onPendingChange`；具体文件处理、预览、确认与操作回执由面板负责。当前已上线功能以 README 为准。

进入前拒绝切换正在读取、写入、有未知结果或编辑草稿的财务工作区。进入时推进请求代次、清空财务显示并暂停后台读取的结果安装；返回后保留查询月份并重新读取当前账本。资产读取报告与来源导入是两个独立面板。

`ScreenProps.onFinanceSourcePending` 把面板的待确认状态接入现有全局导航保护，限制菜单、经典入口、页面离开及浏览器刷新。通知核对真实页面身份和财务路由；旧成员或旧面板回调不能锁住新页面。面板只有先解除自己的 pending 才能调用返回；未知结果如允许保留编号离开，必须由面板明确处理并解除 pending，接线不擅自猜测保存结果。

失焦或身份重置清除入口与导航锁；敏感输入及按成员／家庭隔离的恢复编号由面板处理。此接线不保存文件、预览 token 或财务数据，也不发起业务写入。

接线提交 `39397e9` 仅修改财务页、共享 ScreenProps、HouseholdApp 的导航回调和本文；与已审 UI `efdaeb0`、harness `bf0cbc1`、发布适配器 `d29f15e` 组合。R1 使用真实临时 API 检查导航锁、明确返回保留月份、隐藏／离线、迟到身份和未知结果恢复；12 张截图已由 UI 审查者逐张查看，当前可见区域无阻断；内部滚动区未全覆盖。证据与未验范围见 [组合验收](EXPO-FINANCE-SOURCE-ACCEPTANCE.md)。
