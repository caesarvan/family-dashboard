# 电视单趟旅行回顾：候选使用说明

**候选未上线。** 本文对应组合 `add881ed6a7d455e2743ce2affa6cf8b931b47d2`，不是当前正式站点已具备的功能。生产仍为[助理清单版](ASSISTANT-LIST-ACCEPTANCE.md#assistant-list-release) `e8f0427`／75张户内表＋9张平台表；本候选新安装为77/9。接口以 [TV-TRIP-PLAYBACK](TV-TRIP-PLAYBACK.md)为准。

## 在本地候选开始回顾

1. 沿[开发指南](DEVELOPMENT.md)使用虚构家庭、独立临时数据库与正式同源Expo导出。电视浏览器从 `/tv` 配对；手机成员在「设备与布局」批准所选电视。
2. 打开已保存旅行 → 旅行回顾 →「在电视回顾」。选择一台电视，再选择属于这趟旅行的一条已共享路线，或「不显示路线」。路线可以分页选择。
3. 点击「核对电视内容」，查看旅行标题、日期、获准媒体数量及路线。媒体必须已家庭共享且明确授予这台电视；此步骤不会新增共享或电视许可。
4. 点击「开始电视回顾」。之后可「暂停回顾」「继续回顾」、调整照片间隔、切换媒体，或「返回家庭看板」。两台电视分别选择与控制，不会同时开始。

有可展示路线而没有媒体时也可以开始，此时隐藏上一项／下一项；只有标题且无路线、无媒体不能开始。没选路线就不推断路线。照片和路线同属旅行，不表示照片拍于当前站点；照片按实际展示时长推进，视频沿现有实际播放结束事件推进。

电视路线每页8站、约15秒自动换页；暂停回顾也暂停路线翻页。地图只画服务端给出的相邻允许线段，不跨过不可用站位或隐藏坐标。路线最多100站，页码说明当前覆盖；不连接外部底图或导航服务。

## 结果不明时

先点「核对原开始结果」，查询原 requestId 的本人回执及电视当前状态。读取时 `found:false` 不证明先前请求失败，也不表示旧请求不会稍后完成。

- 页面内仍保有原确认载荷时，「重试本次开始」会先查回执，再严格复用原 requestId／token；不会自动重试。
- 刷新仅保留成员身份、设备ID、requestId；重新进入回顾入口会先核身份，再查原回执。浏览器不保存 token、旅行／路线 DTO 或坐标。
- 若刷新后仍无回执，先读当前电视状态，再由用户明确「重新预览」。这只结束本地旧恢复标识，不取消服务器旧请求；刷新丢失路线选择后默认不显示路线，需重新选择／核对。只有再次点击「开始电视回顾」才会提交新请求，原 revision CAS 防止两个未提交预览互相覆盖。

身份核验失败时隐藏内容并提示重新核对；身份变化清除旧内容及匹配的恢复标识，迟到响应不能重新装入旧数据。不要以换一个 requestId 代替核对未知结果。

## 隐私与失效

路线及地点必须当前仍共享且属于原旅行，作者发起也不会显示私密点或扩大坐标精度。媒体沿原逐台许可和来源授权核验，不会因为选中了旅行而获得额外许可。

路线撤共享、删除或转移后清掉原路线，仍获准的同旅行媒体可继续。旅行删除／关联失效则清掉标题、路线与媒体并保留失效状态，绝不自动换成全部相册。若要播放全相册，须在原设备控制中明确选择；返回家庭看板也会清除旅行范围。

电视标题、路线、照片和视频共同受最长15秒显示租约及约2秒轮询约束。断网、隐藏到后台或身份失效会隐藏内容并停播；服务器拒绝后续未授权读取，但不能收回已交付像素或浏览器冻结画面。真实电视浏览器兼容和用户本人场景尚未验收。

<a id="verification"></a>

## 验证范围与交接

以下是分段证据，不能相加为一次全量检查：

| 固定范围 | 已观察结果 | 限制 |
|---|---|---|
| API `3794a8a` | 94唯一用例分轮最终通过；非作者报告 `tv-trip-ui/test-results/tv-trip-api-independent-r1.json` | 临时Flask／SQLite、合成来源和真实短视频处理；旧fixture失败保留，无Linux／生产声明 |
| UI `6fd938d` | 40唯一Node用例分轮最终通过，两组tsc通过；非作者报告 `tv-trip-api/test-results/tv-trip-ui-review-r1/review.json` | 受控请求与实际TSX处理器，不替代真实双浏览器／实体TV |
| 组合 `add881ed` | fresh npm ci、双tsc、23文件Expo导出均退出0 | build原件 `tv-trip-combination/test-results/expo-tv-trip-combination/build-evidence.json`，SHA `987b147be34065aeeba544001125db3227cfbb7d768fb90e61b2eeecfdd88336` |
| 迁移与初始化 | schema专项32、完整API接缝5、本地演练初版10分别通过 | 原件分别位于 `tv-trip-release/test-results/tv-trip-migration-schema-r1`、`tv-trip-migration-full-api-r2` 和 `tv-trip-linux/test-results/tv-trip-linux-local-r1`；不是实际Linux演练 |
| 当前源索引 | 217个Flask方法／路径、77张户内表、9张平台表 | `tests/inspect_contract.py` 在本次独立临时数据库实际生成，仅含结构，见[路由与表索引](PLATFORM-ROUTES.md) |

原件路径相对私有工作树根目录，未提交截图、数据库、凭据或测试结果到Git。真实组合浏览器尚待终态及独审，后续Linux镜像、75→77迁移／部分失败／完整组恢复、发布计划和生产回读须分别完成。本人完整A–D验收仍0/4，真实云账号及实体电视另验；本地390手机／1920电视浏览器不是物理设备验收。
