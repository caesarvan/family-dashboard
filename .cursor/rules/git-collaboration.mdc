---
description: 家庭看板使用独立 Git 分支和 worktree，审查后集成
alwaysApply: true
---

读取并遵守项目根目录 `AGENTS.md` 与 `docs/GIT-WORKFLOW.md`。

每位 agent 每个任务在独立 `codex/<agent>-<task>` 分支及 worktree 内工作。
禁止多人共用工作目录；由另一位 agent 审查指定提交后，集成人合入 integration，组合验证和再次审查后合入 main。
只提交分配范围的源码、测试及文档；生产凭据、真实财务数据和运行证据不进入 Git。
