# 家庭看板开发约定

先读 `README.md`、`docs/HANDOFF.md`、`docs/GIT-WORKFLOW.md` 和负责模块的接口文档。

## Git 与联合开发

- 每位 agent 每个任务使用独立 `codex/<agent>-<task>` 分支和独立 worktree；开始前核对 Git 根、分支、HEAD 和工作区状态。
- 只编辑任务分配的 worktree 和文件。禁止多个 agent 在同一物理目录编辑、切分支、暂存或合并。
- `main` 保持已审查基线；`codex/integration` 只由集成人操作。模块有共同文件时先约定契约和集成顺序。
- 按职责提交小 commit，交付 base/head、变更范围、实际测试、未验证项。禁止用整目录覆盖代替 Git 合并。
- 由另一位 agent 审查指定 base/head；修复后重新审查。通过后集成人合入 integration，组合验证和再次审查通过后合入 main。
- 未审查或失败的候选留在功能分支；不能把已保存提交写成已上线。当前本地仓库没有托管平台的强制分支保护。
- 不自行推送、创建公开远端、强推、重置他人分支或删除未合入成果。部署由集成人按授权和部署文档执行。

## 数据与验证

- 不提交 `.env`、密钥、数据库、真实财务输入、截图或 `test-results/`。用虚构数据与独立临时数据库测试。
- 个人账户、收入、消费与投资明细只属于本人；只共享已批准的资产/负债汇总。电视只读。
- 保持成员、家庭、CSRF、revision 和异步身份边界；失败保留草稿，重试使用原幂等标识。
- 新模块同步更新接口、数据模型、加载顺序、Docker COPY 和发布白名单。
- 模拟云服务、临时浏览器、源码合并、实际账号和生产部署分别记录。不要运行旧生产写入型 live_check 脚本作为回归。
